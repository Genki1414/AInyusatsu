-- 協力会社の回答ページ（/q/[token]）で、見積金額の代わりに「見送る」「資料請求」を
-- 選べるようにする（タスク4-2の見直し。金額の入力は正式な見積書として弱いため、
-- このページでは受け付けない。実際の見積金額は今後メール返信の取込（タスク4-3）等で記録する）。
--
-- documents_requested : 協力会社が資料の送付を希望した場合true。担当者が手動で資料を
--                        送る運用（既存の実運用）のきっかけとして記録するだけで、
--                        このシステムから資料そのものを配布するわけではない。
alter table quotes add column documents_requested boolean not null default false;
