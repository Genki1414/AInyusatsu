{
  "name": "@ai-nyusatsu-bu/billing",
  "version": "0.0.0",
  "private": true,
  "description": "決済（Stripe）のアダプタ。外部呼び出しはこのパッケージ経由のみ。",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-nyusatsu-bu/domain": "workspace:*",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^3.2.7"
  }
}
