# 調達ポータル（GEPS）巡回 確認事項（未検証）

**この文書は、実際に画面を操作せずに作成した推定を含みます。** 本セッションのネットワーク
ポリシーで `www.p-portal.go.jp` に到達できないため（プロキシで policy denial を確認済み）、
`apps/worker/connectors/geps.ts` のPlaywrightセレクタは実行できていません。

## 0. 確認できたこと（ユーザー提供・確定）

`docs/調達ポータルコネクタ設計.md` §2-5に記載の通り、実機での画面調査が完了しています。

- 検索URL：`https://www.p-portal.go.jp/pps-web-biz/UZA01/OZA0101`
- ログイン不要で検索・資料ダウンロードが可能
- 資料ダウンロードは「連絡先情報をはじめから入力する」でICカード不要（商号・氏名・電話・メールの4項目）
- 1日あたり実測123件（500件の上限に余裕あり）
- 実例（国土交通省 高田管内消融雪設備点検整備業務）で取得できた10ファイルの資料種別対応

## 1. 未確認のまま残っていること（要ローカル確認）

`apps/worker/connectors/geps.ts` の各関数は、§2-5に記載された「確認済みの文言」（分類・
公開開始日・検索・公示本文・調達資料 ダウンロードURL・連絡先情報をはじめから入力する・
商号又は名称／氏名／電話番号／メールアドレス・ダウンロード）を根拠に、Playwrightの
`getByLabel`/`getByRole`等のテキストベースのロケータで実装しました。ただし以下は未確認です。

| # | 事項 | 現在の実装での扱い |
|---|---|---|
| 1 | 「分類」の入力形態（select / checkbox / radio） | selectを試し、失敗したらcheckboxにフォールバックする実装にした。radioの場合は要修正 |
| 2 | 「公開開始日」が開始・終了で別フィールドか単一か | `getByLabel("公開開始日（開始）")`を試し、無ければ`getByLabel("公開開始日")`にフォールバック |
| 3 | 検索結果一覧のテーブル構造・列の並び | 各行から「公示本文」リンクをたどり、同じ`<tr>`内の最初の`<td>`を調達案件番号とみなしている。実際の列構成で崩れる可能性がある |
| 4 | 検索結果の「4つの列」（資料提供招請／意見招請／調達実施案件公示／落札公示）の扱い | 今回は「調達実施案件公示」を主対象とし、他3種の区別は実装していない。落札公示列からの落札結果取得は将来課題（設計書§2-5にも記載あり） |
| 5 | 詳細画面の各項目のDOM構造（ラベルの次の要素が値、という前提） | `getByText(ラベル).locator("xpath=following-sibling::*[1]")`で値を取る実装。実際の構造（dl/dt/dd等）次第で修正が必要 |
| 6 | 「公告内容」が外部リンクかどうかの判定方法 | ラベルの後の最初の`<a>`のhrefを見て、p-portal.go.jp を含まなければ外部リンクとみなしている |
| 7 | 資料一覧（添付資料の一覧）のテーブル構造 | 各行の`<td>`を「項番・資料種別・ファイル名」の順と仮定している |
| 8 | ダウンロードがブラウザの`download`イベントとして発火するか（zipが直接返るか、別画面を挟むか） | `page.waitForEvent("download")`で待ち受ける実装。実際は追加のダイアログ操作が必要な可能性がある |

## 2. 次にやること（優先順）

1. `apps/worker/connectors/geps.ts` を実際にローカルから実行し、`searchByDate` が
   正しい件数（実測123件程度）を返すか確認する
2. 失敗した場合、失敗したステップの関数（`searchByDate` / `scrapeListRows` / `fetchDetail` /
   `downloadDocuments` / `scrapeDocumentCategories`）だけをブラウザの開発者ツールで
   実際のDOMを見ながら修正する（他の関数は独立しているため影響しない）
3. 「調達実施案件公示」以外の3列（資料提供招請／意見招請／落札公示）を収集対象に含めるかを判断する
4. 資料が取得できない案件（外部サイトのみの案件、ICカードが必要な案件）の実例を探し、
   `crawl_errors`への記録方針を固める

## 3. 設計上の判断（変更点）

- **収集端末（IC）は使わない。** ユーザーの指示により、資料ダウンロードは
  「連絡先情報をはじめから入力する」経路のみを実装し、`collector_agents`・
  `fetch_documents_ic` は使わない設計にした
- **機関マスタの自動登録：** GEPSの「調達機関」は実在する発注機関名（例:「北陸地方整備局」）
  であり、KKJの都道府県/市区町村コードとは異なり、そのまま`agencies`の1行として扱える粒度。
  そのため、機関マスタに無い機関は`agencyIdFromName()`（機関名の正規化＋ハッシュ）で
  自動的に`agencies`へ登録する設計にした（`category: "発注機関"`、`sources: []`）。
  KKJでは同様の自動登録を見送ったが（`docs/reference/KKJ_API_確認事項.md` §1参照）、
  データの性質が異なるため判断も変えている
- **資格区分（qual_category）：** GEPSの検索結果・詳細画面からは確定できないため、
  推測せず `"未判定"` とする。AI解析（公告PDFのパース、タスク2-3以降）で確定させる想定
- **提出期限：** 同様に確定できないため、`submit_deadline`等はnullのままとし、
  dedupe_keyの日付部分は`"unknown"`になる（`dedupeKey()`の既定動作）
