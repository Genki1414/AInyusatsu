{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "description": "常駐ワーカー（Railway）。pg-bossでジョブキューを実行する。connectors/jobs はタスク1-5以降で実装。",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "start": "echo \"(未実装) pg-bossの起動はタスク1-5以降で実装\" && exit 0"
  }
}
