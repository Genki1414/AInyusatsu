# ゴールドセット（タスク2-6）

`goldset:template` が作る `goldset.json` を、人が正解で埋めてここに置く。

**このファイルはコミットする。** 公告を1件ずつ読んで作る正解なので、
失うと同じ作業をやり直すことになる。中身は公開されている入札公告の情報で、
秘密にすべきものは入らない。

作りかたと測りかたは `docs/reference/精度の測りかた.md` を見る。
